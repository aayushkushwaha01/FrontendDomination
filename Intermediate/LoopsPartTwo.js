//For Each loop is not considered as loop technically but actually as a method in the array

const myStates=[
    "Uttar Pradesh",
    "Mumbai",
    "Delhi",
    55,
    "Rajasthan",
    "Assam",
];

myStates.forEach( s => (console.log(s)));